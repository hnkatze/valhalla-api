# Secrets and host-specific configuration
.env

# Valhalla data (tiles, PBF, config) is built and synced through S3, never committed
custom_files/

# Terraform
*.tfvars
!*.tfvars.example
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Local skill-registry cache from the agent tooling, not part of the project
.atl/
